# Duplicate artifact adversarial case

This artifact identity is intentionally submitted more than once with different finalized transaction evidence. The second submission must fail atomically because repository ID, commit SHA, and normalized path form a globally consumed key.
