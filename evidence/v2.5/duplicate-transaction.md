# Duplicate transaction adversarial case

This unique artifact is paired with a transaction hash that has already been consumed. The contract must reject the submission atomically before retaining stake, reserving reward capacity, or consuming this artifact key.
