# Semantic rejection evidence

## OBL-001 — Exact campaign binding

The associated escrow transaction uses the configured receipt facts.

## OBL-002 — Full artifact verification

The artifact is immutable and can be fetched completely through the GitHub API.

## Deliberate contradiction

The required settlement-accounting obligation was not completed or documented. No evidence is provided that reward capacity was reserved at submission, consumed on approval, or released on rejection. This omission is intentional and must produce a semantic obligation rejection even though the receipt binding succeeds.
